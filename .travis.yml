language: elixir
elixir:
  - '1.10.3'
otp_release:
  - '23.0'
addons:
  postgresql: '11.7'
service:
  - postgresql
env:
  - MIX_ENV=test
before_script:
  - cp config/travis.exs config/test.exs
  - mix do ecto.create
script:
  - mix do deps.get, ecto.migrate, test
